/*interface is the best place to store the paths of the files and executable.
 * since these path are constant we store it in interface.
 * we implements this interface in our program
 * 
 */
package scripts;

public interface AutoConst
{
	public static final String XL_PATH="./data/input.xls";
	public static final String CD_PATH="./driver/chromedriver.exe";
	
}
